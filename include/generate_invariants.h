#ifndef generate_invariants_h
#define generate_invariants_h

// some class which takes in the neural network object and creates the invaraints out of it

class network_invariants
{
  private:
    int n;
public:
  //
};

#endif
